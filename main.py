from raeder.product import ReadProducts, GetProducts
from raeder.category import ScrapeCategoryProducts, AssignCategory


if __name__ == "__main__":
    # GetProducts()
    # products = ReadProducts().products
    # ScrapeCategoryProducts(product_links=products[4]["products"])
    # AssignCategory()
